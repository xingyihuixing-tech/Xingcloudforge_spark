# AI 组件模块

一旦我所属的文件夹有所变化，请更新我。

## 文件清单

| 文件 | 功能 |
|------|------|
| `_README.md` | 本说明文档 |

> 注：创造模式组件已移除。主入口 `AIAssistantPanel.tsx` 位于 `components/` 根目录。

## 依赖关系

```
AIAssistantPanel.tsx (主入口，位于 components/)
  └─> utils/ai/*.ts (工具函数)
```

## 已移除模块

- `CreatorPanel.tsx` - 创造模式面板（2026-01-02 移除）
